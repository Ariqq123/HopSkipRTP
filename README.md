# HopSkipRTP

Velocity + Paper RTP bridge with Java 21 and Gradle Wrapper.

## Modules

- `proxy`: Velocity command and server selection
- `backend`: Paper-side random teleport execution
- `common`: shared plugin message protocol

## Build

Use `./gradlew build`.
